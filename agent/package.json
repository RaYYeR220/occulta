{
  "name": "occulta-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous strategy runtime for Occulta — the keeper service that reads the sealed policy, computes rebalance intents, and drives confidential netting and settlement on live ETH Sepolia.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:dry": "cross-env DRY_RUN=true node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@iexec-nox/handle": "^0.1.0-beta.13",
    "dotenv": "^17.4.2",
    "ethers": "^6.17.0",
    "viem": "^2.55.2"
  },
  "devDependencies": {
    "@types/node": "^22.11.0",
    "cross-env": "^7.0.3",
    "typescript": "~5.8.0"
  }
}
